# API map
- `/api/v1/health/live` reports process liveness; `/api/v1/health/ready` also requires Gateway.
- Alpha sessions, missions, reports, and professor decisions use bounded `/api/v1` contracts.
- Historical `/api` routes remain compatibility-only during pre-alpha.
