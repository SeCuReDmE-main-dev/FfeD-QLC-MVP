[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ffed-qlc-mvp"
version = "0.1.0"
description = "Public MVP for a bounded FfeD-QLC admissibility layer and Docker/CPAI study-case map."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "LicenseRef-SECL-2.0" }
authors = [
  { name = "SeCuReDmE-main-dev" }
]
dependencies = ["cryptography>=42.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
e2b = ["e2b>=1.0.0"]
api = ["fastapi>=0.110", "uvicorn>=0.29", "pydantic>=2.0", "httpx>=0.27"]
alpha = ["fastapi>=0.110", "uvicorn>=0.29", "pydantic>=2.0", "httpx>=0.27"]

[project.scripts]
ffed-qlc = "ffed_qlc.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
ffed_qlc = ["data/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
